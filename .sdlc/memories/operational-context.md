# Operational context — what's happening right now

> **Rolling snapshot, not a history log.** Hard caps per section below.
> Only `/spec`, `/implement`, `/release` (and explicit block/cancel) move queue lines.
> Machine lines only — no prose bullets. Spec frontmatter `status` is source of truth;
> this file is a denormalized index synced on every transition.

last_updated: 2026-05-29
updated_by: implementer

## todo (max 10)

- _none_

## in_progress (max 10)

- SPEC-0006  intent:INT-0007  adrs:ADR-0007  tracker:plane:-  since:2026-05-29
